export enum routes {
  ABOUT = '/about',
  INDEX_HTML = '/index.html',
  ROOT = '/',
  SETTINGS = '/settings',
  ACTIONS = '/actions',
  INSPECTOR = '/inspector',
  TRACING = '/tracing',
  ANALYTICS = '/analytics',
  PUSH_NOTIFICATION = '/push',
  SNAPSHOTS = '/snapshots',
  CONFIG = '/config',
  COMPRESSION = '/compression',
}
